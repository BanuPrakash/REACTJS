// every member in this file is private, until exported

const  add = (x, y) => x + y;

export default add;

export function subtract(x, y) {
    return x - y;
}

// module.exports = {
//     add,
//     subtract
// }